# AGENTS.md

本文件是本仓库所有 AI 编码代理、自动化开发工具和人工开发者必须遵守的仓库级开发约束。

## 1. 当前开发目标

本分支用于建设 **数据库分发管控服务（Database Distribution Control Service）**。

服务定位：

- 为业务系统、本体服务以及 Agent/Data Access Gateway 提供统一的数据访问与分发入口；
- 负责请求校验、租户与业务域上下文解析、权限判定、幂等控制、数据源路由、适配器调用和审计；
- 首期目标数据源包括 PostgreSQL、MinIO、Redis、Neo4j、Milvus、TimescaleDB；
- 服务本身不得承载具体业务规则，不得成为新的业务数据库，也不得绕过各目标数据源的安全边界。

## 2. 指令优先级

开发时必须依次遵守：

1. 用户在当前任务中的明确要求；
2. 本文件；
3. `docs/governance/vibe-coding-constraints.md`；
4. `docs/architecture/` 下已冻结的架构与契约文档；
5. 代码、测试、迁移脚本和运行配置中已经成立的事实。

发现指令、架构、代码或测试相互冲突时，禁止自行猜测并继续开发。必须先记录冲突、给出影响范围和可选处理方案。

## 3. 强制工作流

任何代码修改都必须按以下顺序执行：

1. 阅读仓库结构、相关架构文档、接口契约、迁移脚本和测试；
2. 输出变更范围、非目标、风险点和验证计划；
3. 先补充或更新契约与测试，再实现代码；
4. 只做完成当前任务所需的最小改动；
5. 执行静态检查、单元测试、契约测试、集成测试和安全检查；
6. 汇报实际执行结果，不得把“预计通过”描述为“已经通过”。

禁止在未理解现有边界时大规模重构、改名、换框架或替换依赖。

## 4. 架构边界

### 4.1 统一入口

所有数据操作必须通过统一入口进入，至少包含：

- `tenant_id`
- `biz_domain`
- `request_id` 或 `trace_id`
- `operation`
- `target`
- `payload` 或查询参数
- 写操作的 `idempotency_key`

不得新增绕过统一入口、租户解析、权限校验、审计或路由层的“临时接口”。

### 4.2 分层职责

建议保持以下单向依赖：

`API/Tool Gateway -> Application Service -> Policy/Validation -> Router -> Adapter Port -> Infrastructure Adapter`

约束：

- API 层只处理协议转换、认证上下文接入和响应封装；
- Application 层负责编排，不直接操作数据库 SDK；
- Router 只根据已验证的目标和策略选择适配器，不执行具体数据库逻辑；
- Adapter 必须实现稳定端口，不得向上泄漏供应商 SDK 类型；
- 业务调用方不得直接依赖具体 Adapter；
- 审计、指标和追踪通过统一接口注入，不散落复制。

### 4.3 数据源适配器

每个数据源必须独立适配，禁止用大量 `if/else` 把不同数据源协议耦合在一个实现中。

每个 Adapter 至少明确：

- 支持的 operation；
- 输入与输出 Schema；
- 超时、重试和熔断策略；
- 事务能力及限制；
- 幂等语义；
- 错误映射；
- 健康检查；
- 审计字段脱敏规则。

## 5. 多租户与安全

- 所有持久化记录、审计记录、缓存键和幂等记录必须显式携带 `tenant_id` 与 `biz_domain`；
- 禁止跨租户 fallback、默认租户、空租户继续执行；
- 数据源、库、Schema、Bucket、Collection、Key 前缀等目标必须来自服务端注册表或 allowlist，禁止客户端直接传入任意连接串或物理地址；
- 禁止拼接 SQL、Cypher、路径、Bucket、对象键或缓存键；
- 参数校验失败、权限不足、目标不存在时必须 fail closed；
- 日志、异常、Trace、测试快照中不得出现密码、Token、连接串、密钥、完整个人敏感信息和未脱敏业务数据；
- 配置中的 Secret 只能通过环境变量或 Secret Manager 注入，不得提交仓库；
- 管理接口和业务接口必须隔离授权策略。

## 6. 契约优先

新增或修改接口前，必须先冻结：

- 请求 Schema；
- 响应 Schema；
- 错误码；
- 幂等语义；
- 超时与重试语义；
- 审计字段；
- 兼容性策略。

禁止让调用方依赖内部异常文本、数据库原始错误或第三方 SDK 响应。

破坏性变更必须使用版本化接口或迁移方案，禁止静默修改现有字段含义。

## 7. 写入与幂等

- 所有写操作必须要求 `idempotency_key`；
- 幂等作用域至少包含 `tenant_id + biz_domain + operation + idempotency_key`；
- 相同幂等键、相同请求体必须返回同一业务结果；
- 相同幂等键、不同请求体必须返回明确冲突；
- 幂等状态需要覆盖处理中、成功、失败和可重试状态；
- 禁止通过“先查再写”假装解决并发幂等问题，必须依靠唯一约束、原子操作或事务；
- 失败重试不得造成重复写、重复事件或重复审计。

## 8. 事务与一致性

- 不默认承诺跨异构数据源强事务；
- 跨源写入必须明确采用 Saga、Outbox、补偿事务或最终一致性策略；
- 每个步骤必须可追踪、可重放、可补偿或明确标注不可补偿；
- 禁止吞掉部分成功；必须返回可诊断状态并形成审计记录；
- 数据库迁移必须向前兼容，提供回滚或恢复说明。

## 9. 错误与可观测性

所有错误必须映射为稳定的领域错误，至少区分：

- 参数或 Schema 错误；
- 认证与授权错误；
- 租户或业务域隔离错误；
- 目标资源不存在；
- 幂等冲突；
- 限流、超时、熔断；
- 下游不可用；
- 部分成功；
- 内部错误。

每次请求必须产生可关联的：

- `request_id` / `trace_id`；
- 结构化日志；
- 访问审计；
- 变更审计；
- 延迟、吞吐、错误率和下游调用指标。

禁止只打印字符串日志或使用 `console.log` 作为正式可观测方案。

## 10. 测试门禁

新增功能至少包含：

- 正常路径；
- Schema 边界；
- 未授权访问；
- 跨租户访问；
- 幂等重复与冲突；
- 并发；
- 超时与重试；
- 下游故障；
- 审计字段；
- Secret 泄漏检查。

Adapter 必须有契约测试；路由与权限策略必须有表驱动测试；关键写路径必须有真实基础设施集成测试或等价的容器化测试。

不得仅以 Mock 测试通过作为“完整验证”。

## 11. Vibe Coding 禁止事项

AI 编码代理不得：

- 为了让测试通过而删除、跳过或弱化测试；
- 修改断言去适配错误实现；
- 使用空实现、固定返回、伪造成功状态或隐藏异常；
- 未经批准新增框架、数据库、消息队列或基础设施；
- 将生产逻辑塞进测试辅助代码；
- 复制粘贴近似实现造成多套真相；
- 在没有迁移方案时直接修改表结构；
- 在没有基准数据时声称性能提升；
- 在没有真实执行时声称部署、测试或安全审计通过；
- 顺手修改与任务无关的文件。

## 12. 完成定义

任务只有在以下条件同时满足时才算完成：

- 需求、边界和非目标已明确；
- 契约和文档已同步；
- 实现遵守分层与租户隔离；
- 测试覆盖新增行为和失败路径；
- 所有声明的检查均真实执行；
- 无明文 Secret、调试代码、临时绕过和未说明的技术债；
- 变更记录包含风险、兼容性和回滚方式。

详细规则见：`docs/governance/vibe-coding-constraints.md`。
